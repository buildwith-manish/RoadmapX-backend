name: Keep Render Alive
on:
  schedule:
    - cron: '*/14 * * * *'  # every 14 mins
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl https://roadmapx-backend-3qmc.onrender.com/me

